
debug / show debug form = false = f1 
generation / segment number of vertices on edge = 50 
generation / segment subdivide if size on screen is bigger than = 1 
generation / segment stop recursion at world size = 100 
generation / planet segments pause subdivision and visibility updates = false 
rendering / enable rasterizer rasterization = true 
rendering / debug / show rasterizer contents = false = f8 
rendering / fullscreen = false 
rendering / debug / pause render prepare = false = p 
rendering / target fps = 60 
rendering / enable culling = true 
rendering / parallelize render prepare = true 
rendering / enable rasterizer culling = true 
rendering / sort renderables = true 
game / walk on planet = false = g 
rendering / debug / reload all shaders = false 
rendering / debug / draw lines = false = f11 
rendering / render only front of triangles = true 
rendering / shadows enabled = false 
rendering / enable post process effects = false 
rendering / debug / draw mesh bouding boxes = false = f7 
rendering / debug / draw normal buffer contents = false = f9 
rendering / debug / draw gbuffer contents = false = f10 
rendering / automatically adjust target fps = true 
rendering / throttle fps = false 
generation / limit generation by fps = true 
generation / print statistics report = false 
generation / use skirts = true 
generation / move skirts on GPU = false 
generation / calculate normals on GPU = true 
rendering / debug / render white background = false 
game / save camera position 1 = false 
game / load camera position 1 = false 
game / save camera position 2 = false 
game / load camera position 2 = false 