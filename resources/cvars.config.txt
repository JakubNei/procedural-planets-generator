debug / save cvars to file = false 
debug / show debug form = false = f1 
game / camera position 1 / load = false 
game / camera position 1 / save = false 
game / camera position 2 / load = false 
game / camera position 2 / save = false 
game / move camera to planet surface on start = false 
game / walk on planet = false = g 
generation / calculate normals on GPU = true 
generation / debug / regenerate all planets = false = r 
generation / limit generation by fps = true 
generation / planet 1 / noise height multiplier = 9000 
generation / planet 1 / noise position multiplier = 0.6371 
generation / planet 1 / radius = 637100 
generation / planet 1 / sea level 0..1 = 0.3 
generation / planet 2 / noise height multiplier = 1000 
generation / planet 2 / noise position multiplier = 0.5
generation / planet 2 / radius = 50000 
generation / planet 2 / sea level 0..1 = 0.3 
generation / planet segments pause subdivision and visibility updates = false 
generation / print statistics report = false 
generation / segment number of vertices on edge = 40 
generation / segment stop recursion at world size = 50 
generation / segment subdivide if weight is bigger than = 0.8
generation / use skirts = true 
planets / debug / get surface height = false 
rendering / debug / draw gbuffer contents = false = f10 
rendering / debug / draw lines = false = f11 
rendering / debug / draw mesh bouding boxes = false = f7 
rendering / debug / draw normal buffer contents = false = f9 
rendering / debug / pause render prepare = false = p 
rendering / debug / reload all shaders = false 
rendering / debug / render white background = false 
rendering / debug / show rasterizer contents = false = f8 
rendering / enable post process effects = false 
rendering / fps / automatically adjust target fps = false 
rendering / fps / fps throttling enabled = false 
rendering / fps / target fps = 60 
rendering / fullscreen = false = f 
rendering / gamma correction = 2.2 
rendering / occlusion culling / enable rasterizer culling = true 
rendering / occlusion culling / enable rasterizer rasterization = true 
rendering / occlusion culling / enabled = true 
rendering / parallelize render prepare = true 
rendering / render only front of triangles = true 
rendering / run render prepare in own thread = true 
rendering / shadows enabled = false 
rendering / sort renderables = true 